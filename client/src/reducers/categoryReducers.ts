import {
  CATEGORY_LIST_REQUEST,
  CATEGORY_LIST_SUCCESS,
  CATEGORY_LIST_FAIL
} from '../constants/CategoryConstants'
import { CategoryAction } from '../types/categoryTypes'

const categoryListReducers = (state) => {}
